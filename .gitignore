.envrc
.env

# Submission tarball — regenerate with `uv run orbit-play pack`
submission.tar.gz

# uv / virtualenv
.venv/

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Worktrees (if any are created by orchestration runs)
.worktrees/
_research_workspace/

# CMA-ES tuning run outputs (generated per-run; user can git add specific
# runs they want to preserve, but default is to ignore)
docs/research_documents/tuning_runs/

#agents
.claude/agents


#blog
/docs/blog
