.envrc
.env

# Submission tarball — regenerate with `uv run orbit-play pack`
submission.tar.gz

# uv / virtualenv
.venv/

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Worktrees (if any are created by orchestration runs)
.worktrees/
_research_workspace/

# CMA-ES tuning run outputs (generated per-run; user can git add specific
# runs they want to preserve, but default is to ignore)
docs/research_documents/tuning_runs/

# Downloaded Kaggle replay JSONs (large; regenerate via
# `uv run python -m tools.download_replays`)
replay/
notebooks/replay/
notebooks/episodes_*.json

#agents
.claude/agents


#blog
/docs/blog

#data
data/

#models
models/




models/
/models
